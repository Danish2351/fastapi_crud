This is a simple fastapi crud application with postgreSQL database. Database we chose is 'northwind' which is public, consists of multiple tables. We have applied Joins queries for retrieval.

![Project Screenshot](ss.png)
